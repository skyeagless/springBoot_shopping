spring.datasource.driver-class-name= com.mysql.jdbc.Driver
spring.datasource.username = root
spring.datasource.password = awpak47m4a1
spring.datasource.url=jdbc:mysql://localhost:3306/mall?useSSL=false&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&autoReconnect=true

mybatis.configuration.map-underscore-to-camel-case=true
#classpath:只能加载找到的第一个资源文件,classpath*:能加载多个路径下的资源文件.
mybatis.mapper-locations = classpath*:mapper/*Mapper.xml
#mybatis控制台日志配置
mybatis.configuration.log-impl= org.apache.ibatis.logging.stdout.StdOutImpl

#mybatis属性（MBG使用)
mybatis.project = src/main/java
mybatis.resources = src/main/resources

#session过期时间
server.servlet.session.timeout=30m

#redis配置
spring.redis.host = 127.0.0.1
spring.redis.port = 6379